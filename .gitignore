# Rust
/target/
**/*.rs.bk
*.pdb

# Secrets — NEVER commit (keys live in the OS keychain via `keyring`)
.env
*.env
secrets.toml

# Local model files (Whisper models are downloaded on first run)
*.bin
*.gguf
models/

# macOS
.DS_Store
# App bundle is a build artifact (produced by scripts/bundle-macos.sh)
/Holler.app/

# Editor
.idea/
*.swp
