# Rust
/target/
**/*.rs.bk
*.pdb

# Secrets — NEVER commit. API keys live in `secrets.toml` in the OS config dir
# (not the repo), but ignore it here too in case one is created locally.
.env
*.env
secrets.toml

# Local model files (Whisper models are downloaded on first run)
*.bin
*.gguf
models/

# macOS
.DS_Store
# App bundle is a build artifact (produced by scripts/bundle-macos.sh)
/Holler.app/

# Editor
.idea/
*.swp

# Personal project planning — stays local, never pushed (Yassir, 2026-06-10)
/docs/
/CLAUDE.md
